import express from 'express';
const router = express.Router();

// Placeholder for cruise routes
router.get('/', (req, res) => {
  res.json({ message: 'Cruise routes placeholder' });
});

export default router;
