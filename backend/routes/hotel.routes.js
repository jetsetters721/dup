import express from 'express';
const router = express.Router();

// Placeholder for hotel routes
router.get('/', (req, res) => {
  res.json({ message: 'Hotel routes placeholder' });
});

export default router;
